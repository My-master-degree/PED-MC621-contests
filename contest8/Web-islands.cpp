#include<bits/stdc++.h>

using namespace std;

int main (){
  int N, 
      M,
      i;
  scanf("%d%d", &N, &M);
  list<int> adj[N];
  int pi[N];

  for (i = 0; i < N; i++)
    c[i] = -1;
  while (N)
}

int _dfs(int N, list<int> * adj, int * c){
  for (int i = 0; i < N; i++)
    if (c[])
    __dfs(N, adj, c);

}


